# System Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER (Browser)                        │
│                    http://localhost:3002                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   NEXT.JS FRONTEND                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ChatInterface│  │DocumentManager│  │ExecutionLogs │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP/REST API
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   FASTAPI BACKEND                            │
│                  http://localhost:8000                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  /query  /upload  /documents  /rebuild-db  /delete  │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  PYTHON AGENT SYSTEM                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Autonomous   │  │  RAG Agent   │  │  Build DB    │      │
│  │    Agent     │  │              │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────┬────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   ChromaDB   │  │  data/ PDFs  │  │  Gemini LLM  │
│  Vector DB   │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
```

## 📤 Document Upload Flow

```
1. USER ACTION
   │
   ├─► Click "Documents" tab
   │
   └─► Select/Drop PDF file
       │
       ▼
2. FRONTEND (DocumentManager.tsx)
   │
   ├─► Validate file type (PDF only)
   │
   ├─► Show upload progress bar
   │
   └─► POST /upload with FormData
       │
       ▼
3. BACKEND (FastAPI)
   │
   ├─► Receive multipart/form-data
   │
   ├─► Save to data/ directory
   │
   └─► Return success response
       │
       ▼
4. FRONTEND
   │
   ├─► Show "Processing..." animation
   │
   └─► POST /rebuild-db
       │
       ▼
5. BACKEND
   │
   ├─► Call build_database()
   │
   └─► Process all PDFs:
       │
       ├─► Extract text (PyPDF)
       │
       ├─► Chunk text (500 chars)
       │
       ├─► Generate embeddings (SentenceTransformers)
       │
       └─► Store in ChromaDB
           │
           ▼
6. FRONTEND
   │
   ├─► Show success message
   │
   ├─► Refresh document list
   │
   └─► Ready for queries!
```

## 💬 Query Processing Flow

```
1. USER TYPES QUESTION
   │
   └─► "What is AI Native Development?"
       │
       ▼
2. FRONTEND (ChatInterface.tsx)
   │
   ├─► Display user message
   │
   ├─► Show loading animation
   │
   └─► POST /query
       │
       ▼
3. BACKEND (FastAPI)
   │
   └─► Call autonomous_agent(query)
       │
       ▼
4. AUTONOMOUS AGENT
   │
   ├─► Create execution plan
   │
   ├─► Log: "Query received"
   │
   └─► Call RAG system
       │
       ▼
5. RAG SYSTEM
   │
   ├─► Convert query to embedding
   │
   ├─► Search ChromaDB for similar chunks
   │
   ├─► Retrieve top N relevant chunks
   │
   └─► Return context
       │
       ▼
6. AUTONOMOUS AGENT
   │
   ├─► Combine query + context
   │
   ├─► Send to Gemini LLM
   │
   ├─► Receive generated response
   │
   └─► Log: "Response generated"
       │
       ▼
7. BACKEND
   │
   └─► Return response + logs
       │
       ▼
8. FRONTEND
   │
   ├─► Display assistant message
   │
   ├─► Update execution logs panel
   │
   └─► Ready for next query
```

## 🗑️ Document Deletion Flow

```
1. USER CLICKS DELETE
   │
   └─► Confirm deletion dialog
       │
       ▼
2. FRONTEND
   │
   └─► DELETE /documents/{filename}
       │
       ▼
3. BACKEND
   │
   ├─► Delete file from data/
   │
   └─► Return success
       │
       ▼
4. FRONTEND
   │
   ├─► Show "Processing..." animation
   │
   └─► POST /rebuild-db
       │
       ▼
5. BACKEND
   │
   └─► Rebuild database without deleted file
       │
       ▼
6. FRONTEND
   │
   ├─► Show success message
   │
   ├─► Refresh document list
   │
   └─► Document removed!
```

## 🔄 Database Rebuild Process

```
REBUILD DATABASE
│
├─► Clear existing ChromaDB collection
│
├─► Scan data/ directory for PDFs
│
├─► For each PDF:
│   │
│   ├─► Extract text
│   │
│   ├─► Split into chunks
│   │
│   ├─► Add source metadata
│   │
│   └─► Generate embeddings
│
├─► Store all embeddings in ChromaDB
│
└─► Database ready for queries
```

## 🎯 Component Interaction

```
┌─────────────────────────────────────────────────────────┐
│                      page.tsx                            │
│  ┌────────────────────────────────────────────────┐     │
│  │  Tab Navigation: [Chat] [Documents]            │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  IF activeTab === 'chat':                               │
│  ┌──────────────────────┐  ┌──────────────────────┐    │
│  │  ChatInterface       │  │  ExecutionLogs       │    │
│  │  - Message list      │  │  - Real-time logs    │    │
│  │  - Input field       │  │  - Color-coded       │    │
│  │  - Send button       │  │  - Auto-scroll       │    │
│  └──────────────────────┘  └──────────────────────┘    │
│                                                          │
│  IF activeTab === 'documents':                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │  DocumentManager                                 │   │
│  │  - Upload area (drag & drop)                    │   │
│  │  - Progress indicator                           │   │
│  │  - Document list                                │   │
│  │  - Delete buttons                               │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

```
PDF Document
    │
    ├─► Text Extraction (PyPDF)
    │   └─► Raw text string
    │
    ├─► Text Chunking
    │   └─► Array of 500-char chunks
    │
    ├─► Embedding Generation (SentenceTransformers)
    │   └─► 384-dimensional vectors
    │
    └─► Vector Storage (ChromaDB)
        └─► Searchable vector database

User Query
    │
    ├─► Query Embedding
    │   └─► 384-dimensional vector
    │
    ├─► Similarity Search (ChromaDB)
    │   └─► Top N relevant chunks
    │
    ├─► Context Assembly
    │   └─► Query + Retrieved chunks
    │
    └─► LLM Generation (Gemini)
        └─► Natural language response
```

## 🌐 Network Communication

```
Browser (Windows)
    │
    │ HTTP Request
    ├──────────────────► Next.js Frontend (WSL)
    │                    Port 3002
    │                        │
    │                        │ Axios HTTP
    │                        ├──────────────► FastAPI Backend (WSL)
    │                        │                Port 8000
    │                        │                    │
    │                        │                    │ Function Call
    │                        │                    ├──────────► Python Agent
    │                        │                    │            │
    │                        │                    │            │ Query
    │                        │                    │            ├──────► ChromaDB
    │                        │                    │            │
    │                        │                    │            │ API Call
    │                        │                    │            └──────► Gemini LLM
    │                        │                    │
    │                        │ JSON Response      │
    │                        │◄───────────────────┤
    │                        │
    │ HTML/JSON Response     │
    │◄───────────────────────┤
    │
    └─► Display to User
```

---

These diagrams show the complete system architecture and data flow for the Autonomous Business Agent Web UI with document upload capabilities.
